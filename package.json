{
  "name": "changsets",
  "description": "monorepo for changsets",
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@changesets/cli": "^2.27.7"
  }
}
